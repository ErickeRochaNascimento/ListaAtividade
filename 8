// Online C compiler to run C program online
#include <stdio.h>

int main() {
    int num,media,soma = 0,contador = 0;
    printf("Digite: ");
    scanf("%f",&num);
    
    
    while(num != 0)
    {
        if(num % 2 == 0){
            soma = soma + num;
            contador++;
        }
        printf("\nDigite: ");
        scanf("%f",&num);
    }
    media = soma / contador;
    printf("A media é %f",media);
    return 0;
}
