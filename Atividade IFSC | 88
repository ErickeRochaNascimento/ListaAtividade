#include <stdio.h>

int main() {
    int num,total = 0;
    for(num = 1; num <= 100; num++)
    {
        total = total + num;;
    }
    printf("A soma dos números de 1 a 100 é: %d\n",total);

    return 0;
}
