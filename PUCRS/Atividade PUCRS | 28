#include <stdio.h>

int main() {
    int n;
    double S = 0.0;
    
    printf("Digite um valor: ");
    scanf("%d", &n);

    for (int i = 1; i <= n; i++) {
        printf("1/%d\n", i);
        S += 1.0 / i;
    }

    printf("S = %.6f\n", S);

    return 0;
}